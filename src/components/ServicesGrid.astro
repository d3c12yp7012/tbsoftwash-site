---
interface Service {
  icon: string;
  title: string;
  description: string;
  color: string;
  link: string;
}

const services: Service[] = [
  {
    icon: '🏠',
    title: 'House Washing',
    description: 'Gentle soft washing for roofs, siding, driveways and entire homes.',
    color: 'blue',
    link: '/services#house'
  },
  {
    icon: '🌞',
    title: 'Roof Cleaning',
    description: 'Remove algae, moss, and stains without damaging delicate shingles.',
    color: 'green',
    link: '/services#roof'
  },
  {
    icon: '🪟',
    title: 'Window Cleaning',
    description: 'Crystal-clear streak-free windows for residential and commercial.',
    color: 'indigo',
    link: '/services#window'
  },
  {
    icon: '🌧️',
    title: 'Gutter Cleaning',
    description: 'Prevent water damage with thorough gutter and downspout cleaning.',
    color: 'amber',
    link: '/services#gutter'
  },
  {
    icon: '🪨',
    title: 'Paver Sealing',
    description: 'Protect and enhance pavers, concrete, and stone surfaces.',
    color: 'rose',
    link: '/services#paver'
  },
  {
    icon: '🎨',
    title: 'Graffiti Removal',
    description: 'Quick removal of graffiti and vandalism without surface damage.',
    color: 'purple',
    link: '/services#graffiti'
  }
];

const colorClasses = {
  blue: 'bg-blue-600 text-blue-600',
  green: 'bg-green-600 text-green-600',
  indigo: 'bg-indigo-600 text-indigo-600',
  amber: 'bg-amber-600 text-amber-600',
  rose: 'bg-rose-600 text-rose-600',
  purple: 'bg-purple-600 text-purple-600'
};
---

<section class="py-20 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Our Services</h2>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">Comprehensive exterior cleaning solutions tailored to your needs</p>
    </div>
    
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {services.map((service) => (
        <div class="bg-white p-8 rounded-xl shadow-md hover:shadow-xl transition-shadow duration-300 group">
          <div class={`w-16 h-16 ${colorClasses[service.color as keyof typeof colorClasses].split(' ')[0]} rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform`}>
            <span class="text-3xl">{service.icon}</span>
          </div>
          <h3 class="text-2xl font-bold text-gray-900 mb-4 text-center">{service.title}</h3>
          <p class="text-gray-600 text-center mb-6">{service.description}</p>
          <a href={service.link} class={`block w-full ${colorClasses[service.color as keyof typeof colorClasses].split(' ')[1]} text-white py-3 px-6 rounded-lg text-center font-semibold hover:opacity-90 transition`}>
            Learn More
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
